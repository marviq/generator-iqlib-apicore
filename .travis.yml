language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-madlib-apicore' ]; then cd .. && eval "mv $currentfolder generator-madlib-apicore" && cd generator-madlib-apicore; fi

